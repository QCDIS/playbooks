

- shell: jq -r '.key | { "{{ item.key }}" :{ key:{id:.id, private_key:.private_key, name:.name} }} ' /tmp/key_output.json
  register: key


- set_stats:
    data:
      instances: "{{ key.stdout }}"
  register: key_stats_output

- debug:
    var: key_stats_output